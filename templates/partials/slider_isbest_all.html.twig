<section class="slider" id="slider">
  <div class="container">
    <h3 class="text-center">Nouveautés</h3>
    <p class="text-center">Découvrez nos derniers articles</p>
    <ul id="autoWidth" class="cs-hidden">
      {# 🔹 On boucle sur les trottinettes #}
      {% for trottinette in trottinettes %}
        <li class="item-a">
          <div class="box">
            <div class="slide-img">
            {% set image = trottinette.illustrations|first %}
            <img src="/uploads/trottinettes/{{ image ? image.image : 'default.jpg' }}" alt="trottinette isBest"/>

              <div class="overlay">
                <a href="{{ path('trottinette_show', { 'slug' : trottinette.slug } ) }}" class="buy-btn">Voir</a>
              </div>
            </div>
            <div class="detail-box">
              <div class="type">
                <a href="{{ path('trottinette_show', { 'slug' : trottinette.slug } ) }}">{{ trottinette.name }}</a>
              </div>
              <a href="{{ path('trottinette_show', { 'slug' : trottinette.slug } ) }}" class="buy-btn">Voir</a>
            </div>
          </div>
        </li>
      {% endfor %}

      {# 🔹 On boucle sur les accessoires #}
      {% for accessory in accessories %}
        <li class="item-a">
          <div class="box">
            <div class="slide-img">
                {% set image = accessory.illustrations|first %}
                <img src="/uploads/accessoires/{{ image ? image.image : 'default.jpg' }}" alt="accessoire isBest"/>
              <div class="overlay">
                <a href="{{ path('accessory_show', { 'slug' : accessory.slug } ) }}" class="buy-btn">Voir</a>
              </div>
            </div>
            <div class="detail-box">
              <div class="type">
                <a href="{{ path('accessory_show', { 'slug' : accessory.slug } ) }}">{{ accessory.name }}</a>
              </div>
              <a href="{{ path('accessory_show', { 'slug' : accessory.slug } ) }}" class="buy-btn">Voir</a>
            </div>
          </div>
        </li>
      {% endfor %}
    </ul>
  </div>
</section>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Bordereau BPOST {{ order.reference }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 0;
            padding: 5px;
            position: relative;
        }
        .bordereau {
            width: 105mm; /* A6 portrait */
            height: 108mm;
            padding: 5mm;
            border: 1px solid #000;
            box-sizing: border-box;
            position: relative;
        }
        h1, h2 {
            margin: 0;
            padding: 0;
        }
        h1 {
            font-size: 14px;
            margin-bottom: 5px;
        }
        h2 {
            font-size: 12px;
            margin-top: 5px;
        }
        .client-info {
            margin-right: 130px; /* espace pour le QR */
        }
        .qr {
            position: absolute;
            top: 5mm;
            right: 5mm;
            width: 120px;
            height: 120px;
        }
        .footer {
            font-size: 10px;
            margin-top: 5px;
            color: gray;
        }
    </style>
</head>
<body>
    <div class="bordereau">
        <h1>BPOST - Commande {{ order.reference }}</h1>

        <div class="client-info">
            <p>Date de la commande : {{ order.createdAt|date('d/m/Y H:i') }}</p>
            <h2>Client</h2>
            <p>
                {% if order.user.phoneNumber is defined %}{{ order.user.phoneNumber }}<br>{% endif %}
                {{ order.delivery|raw }}
            </p>
            <p>Poids total : {{ order.getWeightTotal()|number_format(2, ',', '.') }} Kg</p>
        </div>

        <div class="qr">
            {% if qrCodePath is defined %}
                <img src="{{ qrCodePath }}" alt="QR Code" style="width:120px;height:120px;">
            {% endif %}
        </div>

        <h2>Transport</h2>
        <p>
            Transporteur : {{ order.carrier }}<br>
            Numéro de suivi : <strong>{{ order.trackingNumber }}</strong><br>
            Service : {{ order.shippingMethod|default('Standard') }}
        </p>

        <div class="footer">
            Ce bordereau doit être scanné par le transporteur lors de la prise en charge et à la livraison.
        </div>
    </div>
</body>
</html>
